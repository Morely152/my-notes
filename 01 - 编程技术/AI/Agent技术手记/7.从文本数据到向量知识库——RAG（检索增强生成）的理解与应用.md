伴随着各种大模型的兴起，许多平台开始搭建基于私有知识的AI问答智能体，如售前咨询客服、产品介绍助手、文档快随查询等。这些智能体或多或少都用到了一个共同的技术——RAG（Retrieval-augmented Generation，检索增强生成）。本篇笔记将根据我搭建个人博客RAG模块的经验和学习成果编写，从RAG系统的定义和特点开始，历数文本向量化的实现原理、向量检索匹配的实现原理、通过静态知识库、意图识别、问题轻量改写和关键词/语义匹配加权排序检索等方案优化RAG的知识召回效果等内容。                         
# 一、缘起：为什么我们需要RAG

![](20260611224859969.png)

自2024年AI大模型进入“百家争鸣”的局面以来，国内外涌现出了一大批功能强大、特色各异的大模型，如GPT、Gemini、DeepSeek、豆包、Kimi等。人们逐渐习惯于通过与大模型对话来获取知识，各大平台也开始引入AI客服，应用到售前咨询、文档解读等领域，从而借助AI大模型的智能回答能力让用户快速、准确地获得问题解答，用户就无需像往常一样去翻阅冗长复杂的文档。于是RAG技术应运而生——让大模型“读懂”私有知识，从而增强它输出内容的质量。
## 1.通用大模型如何“读懂”私有知识

“读懂”这个词似乎有点难以理解，这里举一个具体的例子来说明。想象一下我们给一家保险公司的网站接入了AI大模型，用户可以在网站上提问，问题交给大模型进行回答。

```
用户：你好！
        ↓
---- 保险公司系统后台 -------------
用户提示词：你好！
系统提示词：你是一位XX保险公司的专业
客服，请你以专业热情的态度解决用户提
出的问题。
-----------------------------------
        ↓
---- AI大模型 ---------------------
返回回答：您好，我是XX保险公司的客服
，请问您需要咨询什么内容？
-----------------------------------
```

某天用户询问了“我可以选购哪些保险服务？”，结果系统背后的大模型直接上网查询了其他公司的保险政策文件，瞎编了一大串不属于甲方公司的服务，因此我们的AI智能客服遭到了用户的投诉，甲方责令我们进行修复。

为了解决这个问题，我们提取了`《XX保险公司用户协议合同》.pdf`中的文本，将其添加到了系统提示词中，现在AI大模型就会参考协议合同中的内容进行回答：

```
用户：我可以选购哪些保险服务？
        ↓
---- 保险公司系统后台 -------------
用户提示词：我可以选购哪些保险服务？
系统提示词：你是一位XX保险公司的专业
客服，请你以专业热情的态度解决用户提
出的问题。以下是公司的用户协议合同，
需要参考其中的内容进行解答，协议合同
中没有用户问题相关的内容时，严禁参考
网络知识或者训练知识进行解答，需要进
行拒绝回答，并引导用户询问其他相关的
内容。
附件：《XX保险公司用户协议合同》.txt
-----------------------------------
        ↓
---- AI大模型 ---------------------
返回回答：根据我司用户协议合同内容，
您可以投保以下类型的保险服务：...
-----------------------------------
```

通过直接把文档**全文**添加到提示词当中，现在的AI智能客服就学会了参考我们提供的私有知识来回答，而非参考不准确的知识，从而出现“幻觉”现象。

> **大模型幻觉（LLM Hallucination）** 指大语言模型在生成文本时，产生与用户输入相悖、与已有上下文矛盾、或与真实世界知识不符内容的现象。术语来自神经科学，类比大脑在无外部刺激时自发产生感知的病理状态。[^1]

![](20260611215831990.png)

甲方很高兴，通过了验收。事实上这样就形成了RAG的雏形——给大模型提供知识库，使大模型从中检索相关内容作为参考，从而准确回答用户的问题。但这毕竟是雏形，事实上一个成熟的RAG系统比这更加复杂。

## 2.给大模型写“说明书”：构建知识库

一段时间后，甲方业务范围扩展，从原来只做人身保险增加了财产保险，下设企财险、工程险、车险、责任险、船舶险、货运险、家财险、信用险、保证险、农险、巨灾保险、新能源汽车专属保险[^2]等一大堆业务，而且各自有对应的保险政策和用户协议，现在需要把这些内容同步给AI智能客服。

看着这么几十个文件，现在把它们全部放到提示词里面，每次都发给AI显然不是一个明智的办法。一方面用户问题只涉及某些文件中的一些内容，每次都发全量文档参考会造成不必要的流量和Token支出；一方面大模型基于数学概率，让它在海量知识中检索相关内容也存在很多不确定性，不能保证每次都能找到有用的知识。

既然如此，我们选择把每篇文章都切碎成一些文本块，然后贴上标题、分类、时间等标签（元数据），并且转换为**向量数据**存入向量数据库中存储起来。当用户提问时，由系统来检索其中相关的内容，然后把这些内容发送给大模型进行参考。这样就相当于给大模型定制了一本“说明书”，大模型得以参考其中相关的知识。

![](20260611220956957.png)

## 3.查找“说明书”：使用向量检索

为什么要将文本块转换为向量？为了知道发送哪些相关的知识文本块给大模型进行参考，系统需要具备从知识库中检索相关内容的能力。需要注意这里的“相关”指的是**语义上**的相关性，而不是字面意义上的匹配。因此使用关键词匹配显然不够，需要内置一个embedding模型将文本转换为一系列特征值，组成**高维向量**，语义相似的文本对应的向量，在高维空间中的位置也越近。在RAG系统中，通常使用余弦相似度算法，即通过计算两个向量夹角的余弦值来衡量它们的相似性，其值越接近1表示越相似，越接近-1表示越不相似。

于是，对于切分好的文本块，我们使用embedding模型将其转换成向量，即一串表示文本特征的数字，随着文本块一起存入向量数据库中，这样就构建了更加智能的知识库——向量知识库。

![](20260611223314024.png)
## 4.RAG概述的总结

通过上述分析，我们了解到了传统的将文档直接塞进提示词的做法，会随着文档内容的不断增加而被淘汰，进而采取将海量文本切块，进行向量化处理的方法来构建向量知识库。用户提问时，系统也抽取问题的向量，与知识库中的向量进行相似度匹配，达到从语义的角度来检索相关内容的目的，而不是简单检索包含问题关键字的相似内容。了解了RAG的大致流程后，我们接下来将更加详细地分析从知识入库到向量检索整个过程的原理与实现。接下来，我们将深入了解这一过程，以及整个RAG模块是如何在我的博客系统中实现的。
# 二、构建：从文本到知识库的实现过程

## 1.embedding模型简介

我们需要实现这样的处理：将一些句子，根据它们的意思（语义）来“定位”到一个坐标系中；意思越接近，它们的位置就越靠近。这样当我们需要在知识库中查找跟用户的问题“意思接近”（语义相似）的知识时，先“定位”用户问题在坐标系中的位置，然后返回问题附近的一些句子就可以了。这样的工具称为“**embedding模型**（一般称作嵌入模型、向量化模型）”。与大语言模型LLM不同，它接收到句子之后，输出的不是句子，而是一组元素个数相同的浮点数数组。数组中的这些元素（一般有成百上千个）可以对应一个高维空间中的坐标，于是我们将这个由很多元素组成的向量称为“**高维向量**”，数组中元素的个数称为向量的维度。

对于同一个embedding模型而言，无论输入的句子长度如何，它输出的高维向量的维度是固定的，并且向量中各个维度的分量含义也是对应相同的。例如在一个embedding模型中，第1维可能专门负责探测“情绪积极程度”；第5维可能专门负责探测“文本是否涉及科技领域”。第233维可能专门负责探测“句子的复杂程度”。这样使得由同一个embedding模型转化而来的一些向量之间具有了良好的一致性。

例如，我们有一个简易的embedding模型，它会把每个输入的句子转化为二维向量，两个维度分别为 `(情绪积极程度, 科技相关程度)`，取值范围 `[-1, 1]`。根据这样的规则，对下面三句话进行处理：

|句子|输出向量|解读|
|---|---|---|
|**我喜欢吃苹果**|**(0.8, -0.3)**|情绪积极（喜欢），但"吃苹果"是生活场景，几乎不涉及科技，所以科技维度偏负|
|**我不喜欢学编程**|**(-0.6, 0.9)**|情绪消极（不喜欢），但"编程"是典型科技活动，所以科技维度很高|
|**苹果手机好用**|**(0.7, 0.8)**|情绪积极（好用），且"苹果手机"是科技产品，科技维度也高|

当用户问系统“我喜欢吃什么？”的时候，embedding模型认为这个问题情绪很积极，与科技不相关，得到`(0.5，-0.5)`的向量，于是就会寻找这个位置附近的知识，从而发现“我喜欢吃苹果”，返回了“苹果”的回答。

![](20260617092900994.png)

下表列出了embedding技术的发展历程中，一些具有代表性的embedding模型：

|模型|输出维度|核心特点|常见应用领域|
|---|---|---|---|
|**Word2Vec**|通常100~300维（可自定义）|第一代静态词向量模型，一个词对应一个固定向量，无法处理多义词和上下文信息。|词相似度计算、传统NLP任务、Embedding概念教学。|
|**BERT**|Base：768维  <br>Large：1024维|上下文感知Embedding模型，可根据语境动态生成词向量，解决多义词问题。原生输出为Token级向量。|问答系统、文本分类、信息抽取、语义理解任务。|
|**Sentence-BERT（SBERT）**|通常384~1024维|在BERT基础上专门优化句向量生成，适合直接计算文本相似度。|语义搜索、文本聚类、文本匹配、RAG召回。|
|**E5系列**|Base：768维  <br>Large：1024维|微软推出的高性能开源Embedding模型，对检索任务进行了专门优化。|企业知识库检索、RAG系统、向量数据库。|
|**OpenAI text-embedding-3-small**|1536维|OpenAI当前主流Embedding模型之一，兼顾成本、性能与多语言能力。|通用语义搜索、RAG、多语言检索。|
|**OpenAI text-embedding-3-large**|3072维|OpenAI高性能Embedding模型，语义表达能力更强。|高精度知识检索、复杂RAG系统、多语言应用。|
|**BGE系列**|Base：768维  <br>Large：1024维|智源研究院推出，中文和英文检索表现优秀，是当前开源RAG领域主流选择之一。|中文知识库检索、语义搜索、企业RAG系统。|
|**BGE-M3**|1024维|支持多语言、多粒度、多检索模式（Dense/Sparse/Multi-Vector），兼顾检索效果与灵活性。|下一代RAG系统、跨语言知识库、多模态检索。|
|**M3E系列**|Small：512维  <br>Base：768维|面向中文优化的轻量级Embedding模型，在中文检索和文本匹配任务中表现优秀。|中文RAG、文本相似度计算、知识库检索。|
|**Qwen3 Embedding**|1024~3072维（不同版本）|阿里通义千问推出的新一代Embedding模型，中文能力突出，同时支持多语言。|中文企业知识库、Agent检索增强、RAG系统。|
|**GTE系列（General Text Embeddings）**|768~1024维|阿里达摩院开源Embedding模型，检索效果稳定，适合工业场景。|企业搜索、语义匹配、知识库检索。|
|**all-MiniLM-L6-v2**|384维|轻量高效，推理速度快，占用资源少。|实时检索、大规模向量搜索、边缘部署。|
|**Jina Embeddings v3**|512~1024维（可裁剪）|支持100+语言，长文本能力强，采用Matryoshka表示学习，可灵活压缩维度。|多语言RAG、长文档检索、国际化应用。|
|**CLIP**|512~768维（视版本而定）|图文联合Embedding模型，将图片和文本映射到同一向量空间。|文搜图、图搜图、多模态检索。|
|**SigLIP**|768维左右（视版本而定）|Google提出的新一代图文Embedding模型，相较CLIP具有更好的检索性能。|图文搜索、多模态RAG、视觉语义理解。|

对于一些常见的业务场景，推荐使用的模型如下（仅供参考）。实际业务中，选用哪种模型既取决于知识库容量大小、知识是纯文本或多模态、知识领域（例如法律医学、科学技术、文档说明）等环境因素，也要考虑预算有限时的服务器性能限制（这一点上我踩过雷，不要直接上最好的模型，因为服务器不一定带得动）。

| 场景      | 推荐模型                                  |
| ------- | ------------------------------------- |
| 中文RAG   | BGE-M3、BGE-large-zh、Qwen3 Embedding   |
| 英文RAG   | E5-large、BGE-large-en                 |
| 多语言RAG  | BGE-M3、Jina v3、text-embedding-3-large |
| 企业私有知识库 | BGE-M3、Qwen3 Embedding、E5-large       |
| 轻量级部署   | all-MiniLM-L6-v2、M3E-small            |
| 图文混合知识库 | SigLIP、CLIP                           |
## 2.文本向量化原理：从文字到数字

文本向量化技术的演进过程中，衍生出了如下三个层层递进的核心范式：
### ① 第一阶段：上下文无关（Context-Free）—— 静态表示

此阶段的代表技术有One-hot（独热编码）、Bag-of-Words（词袋模型）、TF-IDF等。
#### One-Hot独热编码

One-Hot编码，又称为一位有效编码。这样的编码将一句话中各个特征的取值枚举出来，以二进制的方式进行编号。类似于定义了一份”座位表“，为各个特征的所有取值都确定好了对应的编号。这样在处理文本时，先把文本分词得到一系列特征词，然后逐个查询它们在”座位表“中的One-hot编码，从而得到对应的向量，拼在一起作为整个文本转换而来的向量：

```text
One-Hot编码向量化示例
---------------------
做出如下规定：
	性别：男性（10），女性（01）
	国籍：中国（100），美国（010），法国（001）
	运动：足球（1000），篮球（0100），羽毛球（0010），乒乓球（0001）
  
那么对于“一位男性中国乒乓球员”：
	系统会将其分词为["男性","中国","乒乓球"]，
	从而得到向量[1，0，1，0，0，0，0，0，1]。
```

需要说明的是，在传统的机器学习（如逻辑回归、决策树）中，类别特征是没有大小顺序的。如果把“足球”编码成1，“篮球”编码成2，“羽毛球”编码成3，算法就会误以为“足球 > 篮球 > 羽毛球”，即引入了错误的数值大小关系。而One-Hot通过强制让两两正交（向量夹角余弦=0），明确定义“足球”、“篮球”、“羽毛球”是三个完全独立、平级、不存在大小优劣之分的类别。这正是One-Hot编码最本质的设计初衷。

不足的是，在One-Hot编码中，每个词被表示为一个唯一固定的向量（类似于统一的词典），它的取值与上下文完全无关。这样的编码策略使得One-Hot无法解决一词多义问题。例如，“苹果”这个词在“吃苹果”和“苹果手机”中向量完全一样。
#### Bag-of-Words（词袋模型）

词袋模型的基本概念是将每个文档视为无序词汇集，只考虑每个词语的出现频率，将出现频率作为文本的特征地图。类似于拿一个空袋子，把文章中的词汇都掰碎丢进去，

#### TF-IDF

### ② 第二阶段：词嵌入（Word Embeddings）—— 静态稠密表示

代表技术：Word2Vec（CBOW/Skip-gram）、GloVe、FastText。
核心逻辑：将词映射为稠密、低维的实数向量，并通过向量空间中的距离（如余弦相似度）来度量词义相似性（如“国王-男人+女人≈女王”）。
局限性：虽然引入了语义关联，但仍然属于上下文无关。因为训练好后，每个词仍然只有一个固定的向量，无法根据句子语境动态变化。
本质：这是一种基于分布假设的预测性表示，是静态嵌入的巅峰。

### ③ 第三阶段：基于上下文（Context-Based）—— 动态表示

代表技术：ELMo、GPT、BERT 等基于 Transformer 的预训练语言模型。
核心逻辑：词的向量不再是静态的，而是根据其所在的实时上下文（句子中的其他词）动态生成。同一个词在不同的句子里会获得完全不同的向量。
革命性突破：彻底解决了一词多义问题。例如，BERT 会根据“苹果”周围是“吃”还是“手机”来动态计算向量。
本质：这是一种基于深层双向/自回归语言模型的动态表示，向量包含了句法和语义的深层交互信息。


（一词多义的理解）
## 3.常用embedding模型列举




## 4.长文本的处理：chunk




# 三、检索：在知识库中进行向量相似度匹配



拓展：AI输出的约束与权限分级、元数据的应用
# 四、优化：为什么知识库的检索效果不好

# 五、测评：如何评估RAG的有效性

# 六、回望：学习心得与系统复盘

```
你说每篇文章先切块，再转化化为512维的向量，那么这些向量是如何转化得到的？
这些向量对应的每个维度是否有确切的含义？或者说对应维度的意义是否相同（比如说三维向量中，三个维度的值对应的方向轴是一致的）？
你用到了umap算法来降维，那么降维之后的坐标在三维空间中的位置关系是否代表原来的向量之间的语义相关性？
你提到基于langchain来调度文本嵌入模型，具体是怎么做的？
系统检索不到相关的内容/检索到不相关内容/出现脱离事实或错误内容的情况下如何进行优化处理？
```

（举例向量化与RGB分色的相似性）

- 回顾全流程：从RAG概念、向量化、知识库构建、检索优化到可视化管理，形成闭环。
- 个人感悟：AI Coding工具能帮你快速“做出来”，但要“讲清楚”，必须回归基础原理的学习。
- 对同为初学者的读者的建议：不要止步于调库，试着回答“这个向量是怎么来的”、“这个维度是什么意思”这样的问题，才能真正理解RAG。


--- 
# 参考资料

（本文插图由 GPT-Imgae-2 模型根据原文内容生成，部分示例优化和细节补充由 DeepSeek 协助完成，仅用于辅助理解文章内容）

[^1]: 思否（@七牛云行业应用）：大模型幻觉是什么？定义、类型、成因与 2026 年最新缓解方案 - https://segmentfault.com/a/1190000047657867
[^2]: 百度百科：保险种类 - https://baike.baidu.com/item/%E4%BF%9D%E9%99%A9%E7%A7%8D%E7%B1%BB
[^3]: 哔哩哔哩（@隔壁的程序员老王）：这就是RAG 一看就懂的个人知识库架构 - https://www.bilibili.com/list/watchlater?bvid=BV19RJhzyEWN
[^4]: 哔哩哔哩（@马克的技术工作坊）：RAG 工作机制详解——一个高质量知识库背后的技术全流程 - https://www.bilibili.com/list/watchlater?bvid=BV1JLN2z4EZQ
[^5]: 哔哩哔哩（@图情充电站）：半小时搞懂社科C刊中的文本向量化方法！ 从TF-IDF到Doc2vec再到Sentence-BERT | 零基础GET文本挖掘系列 - https://www.bilibili.com/video/BV1DM4m1S76E
[^6]: 博客园（@charygao1990）：机器学习：数据预处理之独热编码（One-Hot）详解 - https://www.cnblogs.com/Chary/p/19326196
[^7]: 知乎（@Miracles​）：传统NLP之Bag of Words（词袋模型） - https://zhuanlan.zhihu.com/p/667280452